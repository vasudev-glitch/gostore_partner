name: local_auth
description: Local override of compileSdk = 35 plugin
version: 1.0.0

flutter:
  plugin:
    platforms:
      android:
        package: io.flutter.plugins.local_auth
        pluginClass: LocalAuthPlugin

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  local_auth_platform_interface: ^1.0.10
